mutation createEvent($event: CreateEventInput!) {
    createEvent(event: $event) {
        id
        type
        subType
        startTime
        endTime
        draft
        priority
        metadata
    }
}